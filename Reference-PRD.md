# Reference — PRD (제품 요구사항 문서)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.2 |
| 작성일 | 2026-09-17 |
| 대상 독자 | 개발자 · 디자이너 |
| MVP 목표 | 1개월 *(가정)* |
| 함께 보는 문서 | `Reference-DESIGN.md` (디자인 명세) |

> **v0.2 변경 요약** — 분류 체계가 확정되었습니다. **카테고리 6개 고정 → 그 안에 사용자가 만드는 폴더**로 계층이 정해지면서, v0.1에서 열려 있던 질문 4번("폴더와 카테고리 둘 다 필요한가")이 해소되었습니다. 이에 따라 흐름·기능·완료 기준·범위 제외를 전부 다시 정리했습니다.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| **제품 이름** | Reference (가칭) |
| **한 줄 설명** | 친구끼리 찾은 이미지와 링크를 한곳에 모아, 서로 실시간으로 확인하고 이야기할 수 있는 웹 사이트 |
| **버전 목표** | MVP(최소 기능 제품 — 핵심 기능만 담은 첫 버전) 1개월 내 출시 *(가정)* |

### 만드는 이유

지금은 각자 마음에 드는 사진이나 링크를 찾으면 개인 폴더, 카카오톡 대화방, 핀터레스트 보드 등 **제각각 다른 곳에** 모읍니다. 그러다 보니

1. 상대방이 뭘 모았는지 그때그때 볼 수 없고,
2. 카톡에 올린 이미지는 시간이 지나면 위로 밀려서 찾기 어렵고,
3. "이거 어때?" 같은 의견은 이미지와 따로 놀아서 나중에 보면 무슨 이야기였는지 모릅니다.

Reference는 **"모으는 곳 = 보는 곳 = 이야기하는 곳"을 하나로 합쳐서**, 내가 올리는 즉시 친구가 보고 바로 댓글로 반응할 수 있게 만듭니다.

### 무엇을 모으는가 — 스냅사진 레퍼런스

이 보드는 **촬영 컨셉을 정하기 위한 스냅사진 참고 이미지**를 모읍니다. 그래서 분류 기준을 "아무 이름이나 붙이는 폴더"가 아니라 **실제 촬영 종류 6가지로 처음부터 고정**했습니다. 고를 필요 없이 이미 나뉘어 있어야 저장이 빨라지기 때문입니다.

```
보드
├── 1. 개인          ← 카테고리 6개, 고정 (추가·삭제 없음)
├── 2. 커플
├── 3. 친구·우정
├── 4. 일상스냅
├── 5. 셀프웨딩
└── 6. 웨딩스튜디오
        └── 야외 / 실내 / 필름톤 …   ← 폴더, 사용자가 직접 생성 (1단계까지만)
```

- **카테고리**는 6개로 고정입니다. 이미지 1장은 카테고리 1개에만 속합니다.
- **폴더**는 카테고리 안에서 사용자가 만듭니다. 이미지 1장은 폴더 0~1개에 속합니다(안 넣어도 됩니다).
- 폴더 안에 폴더는 만들 수 없습니다.

---

## 2. 대상 사용자

주 사용자는 **2~5명 규모의 친한 그룹 하나** *(가정 — "친구, 나"라는 답변을 기준으로 소규모 비공개 그룹으로 전제)* 입니다.

### 페르소나 A — 지훈 (26세, 그룹을 만든 사람)

| 항목 | 내용 |
|---|---|
| **상황** | 친구와 함께 자취방 인테리어를 준비 중. 인스타그램·핀터레스트·블로그를 돌아다니며 하루 10~20장의 이미지를 발견한다. |
| **원하는 것** | 발견한 순간 3초 안에 저장하고 싶다. 나중에 "주방", "조명"처럼 나눠서 보고 싶다. |
| **불편한 점** | 카톡 "나에게 보내기"에 쌓아두니 며칠 뒤엔 스크롤로 찾을 수가 없다. 친구한테 다시 보내주는 것도 일이다. |

### 페르소나 B — 수아 (27세, 초대받은 사람)

| 항목 | 내용 |
|---|---|
| **상황** | 앱을 새로 깔거나 회원가입하는 걸 귀찮아한다. 주로 스마트폰으로 본다. |
| **원하는 것** | 링크 하나 눌러서 바로 들어가, 지훈이 뭘 올렸는지 훑어보고 "이건 좀 아닌 듯" 한마디 남기고 싶다. |
| **불편한 점** | 의견을 남길 데가 마땅치 않아 결국 카톡으로 말하는데, 이미지와 연결이 안 돼서 대화가 겉돈다. |

---

## 3. 해결할 문제

### 3-1. 문제 상황

1. **실시간성 없음** — 각자 따로 모으니, 친구가 지금 뭘 모았는지 물어보기 전엔 알 수 없다.
2. **정리 안 됨** — 대화방에 올린 이미지는 시간순으로만 쌓여, 주제별로 다시 보기가 불가능하다.
3. **링크가 안 보임** — 링크만 공유하면 파란 글씨 하나뿐이라, 열어보기 전엔 뭔지 모른다.
4. **의견이 흩어짐** — 이미지는 A에, 그에 대한 반응은 B에 남는다.

### 3-2. 지금은 어떻게 해결하고 있나 (현재 대체 수단)

| 방법 | 되는 것 | 안 되는 것 |
|---|---|---|
| 카카오톡 단톡방 | 즉시 공유, 알림 | 시간순뿐 / 검색·분류 불가 / 이미지 만료 |
| 각자 PC 폴더 | 정리 잘 됨 | 상대방이 볼 수 없음 |
| 핀터레스트 공유 보드 | 이미지 정리 | 핀터레스트 밖 이미지·링크는 넣기 번거로움, 가입 필요 |
| 구글 드라이브 공유 폴더 | 파일 공유 | 썸네일 보기 불편, 댓글이 이미지와 안 붙음 |

> **빈칸:** "즉시 공유 + 주제별 정리 + 이미지·링크 모두 + 그 자리에서 댓글"을 한 번에 해주는 도구가 없다.

---

## 4. 사용자 흐름

### 흐름 A — 보드 만들고 초대하기 (최초 1회)

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| A-1 | 첫 화면(랜딩) — "보드 만들기" 버튼 하나 | 버튼 클릭 |
| A-2 | 이름 입력 창 (닉네임만, 비밀번호 없음 *(가정)*) | 닉네임 입력 → 시작 |
| A-3 | 빈 보드 + "초대 링크 복사" 버튼 | 링크 복사해서 카톡으로 친구에게 전달 |
| A-4 | (친구 화면) 링크 열면 닉네임 입력 창 | 닉네임 입력 → 같은 보드에 입장 |

### 흐름 B — 카테고리 고르기 (모든 작업의 출발점)

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| B-1 | 보드 홈 — 큰 제목 `SNAP REFERENCE` + `[총 342장]` | 훑어봄 |
| B-2 | 그 아래 **6개 카테고리 카드가 한 줄로** 배열. 각 카드는 대표 사진 1장 + 이름 + `[장수]` | 원하는 컨셉 선택 (예: "셀프웨딩") |
| B-3 | 새 사진이 올라온 카테고리는 이름 옆에 파란 점 표시 | 점이 있는 카드를 우선 확인 |
| B-4 | 카테고리 상세 화면으로 이동 — 사진 그리드 + 폴더 칩 + "사진 올리기" | 여기서 흐름 C·D·E·F로 이어짐 |

### 흐름 C — 이미지 올리기 (핵심 흐름)

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| C-1 | 카테고리 상세 화면 (예: "셀프웨딩 [52]") | (선택) 넣을 폴더 칩을 먼저 클릭 |
| C-2 | 화면 아무 곳 | 이미지 파일을 드래그해서 끌어다 놓기 (또는 "↑ 사진 올리기" → 파일 선택) |
| C-3 | 전체 화면 안내 — **"여기에 놓으면 [셀프웨딩 · 야외]에 올라갑니다"** | 놓기 |
| C-4 | 그리드 맨 앞에 자리가 먼저 생기고 진행 바 표시 → 완료되면 사진으로 바뀜 | 확인 |
| C-5 | 사진에 올린 사람 닉네임·시간 표시 (마우스를 올리면 보임) | — (여기서 "행위 완료") |

> C-3의 문구에 **카테고리와 폴더 이름을 박아 넣는 것**이 이 흐름의 핵심입니다. 어디에 올라가는지 놓기 전에 알려주면, 잘못 올려놓고 다시 옮기는 일이 없어집니다.

### 흐름 D — 링크 올리기

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| D-1 | 카테고리 상세 화면 | URL 복사해서 붙여넣기 (`Ctrl+V`) 또는 링크 입력창에 Enter |
| D-2 | 자동으로 제목·설명·대표 이미지를 불러와 미리보기 카드 생성 (OG 태그 — 웹페이지가 공유용으로 미리 넣어둔 대표 이미지/제목 정보를 읽어옴) | 확인 |
| D-3 | 이미지 추출 실패 시: 회색 기본 카드 + "이미지 직접 넣기" 버튼 | 그대로 저장하거나 이미지 직접 첨부 |

### 흐름 E — 보고, 말하고, 받기

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| E-1 | 그리드에 친구가 방금 올린 사진이 파란 테두리로 4초간 표시됨 | 사진 클릭 |
| E-2 | 상세 보기(원본 크게 보기 + 오른쪽에 업로더·폴더·댓글 + 다운로드) | 댓글 입력 "이거 좋다" |
| E-3 | 댓글이 오른쪽 패널에 즉시 표시 | `←` `→` 키로 다음 사진 확인 |
| E-4 | 다운로드 버튼 클릭 | 원본 이미지가 기기에 저장됨 |

### 흐름 F — 카테고리 안에서 폴더로 다시 나누기

| 단계 | 사용자가 보는 화면 | 사용자가 하는 행동 |
|---|---|---|
| F-1 | 카테고리 상세 상단의 폴더 칩 행 — `전체 / + 폴더` | `+ 폴더` 클릭 |
| F-2 | 그 자리에 바로 입력창이 열림 | 폴더 이름 입력 (예: "야외", "필름톤") → Enter |
| F-3 | 칩 행에 새 폴더가 추가됨 | 분류할 사진들을 체크 (또는 드래그로 여러 장 선택) |
| F-4 | 하단에 액션 바 — `12장 선택 · 폴더로 이동 ▾ · 다운로드 · 삭제` | "폴더로 이동" → 폴더 선택 |
| F-5 | 폴더 칩 클릭 | 해당 폴더 사진만 그리드에 표시 (`전체`로 되돌릴 수 있음) |

> **폴더와 카테고리의 차이 (확정)**: **카테고리**는 6개로 미리 정해진 촬영 종류이며 바꿀 수 없습니다. **폴더**는 그 안에서 사용자가 직접 만드는 하위 분류입니다. 예: `셀프웨딩 > 야외`. 한 사진은 카테고리 1개, 폴더 0~1개에 속합니다.

---

## 5. 기능 요구사항

### 5-1. 반드시 필요 (Must)

| 기능명 | 설명 | 우선순위 | 관련 흐름 |
|---|---|---|---|
| 보드 생성 & 초대 링크 | 보드를 만들면 고유 URL이 생기고, 그 링크를 아는 사람만 들어올 수 있다 | Must | A-1~A-4 |
| 간편 참여 (닉네임) | 이메일·비밀번호 없이 닉네임만으로 참여 *(가정)* | Must | A-2, A-4 |
| **6개 고정 카테고리** | 개인 / 커플 / 친구·우정 / 일상스냅 / 셀프웨딩 / 웨딩스튜디오. 보드 홈에 한 줄로 배열 | Must | B-2 |
| **카테고리 대표 이미지** | 각 카드에 해당 카테고리의 최신 업로드 1장을 자동 표시. 비었으면 회색 + `[비어 있음]` | Must | B-2 |
| **카테고리별 장수 표시** | 카드 아래 `[64]` 형태로 현재 장수 표시 | Must | B-2 |
| **카테고리 상세 진입** | 카드 클릭 → 해당 카테고리의 사진 그리드 화면으로 이동 | Must | B-4 |
| **폴더 생성/이름변경/삭제** | 카테고리 **안에서** 폴더를 만든다. 폴더 삭제 시 사진은 지워지지 않고 `전체`로 빠진다 | Must | F-1, F-2 |
| **폴더 필터** | 폴더 칩을 클릭하면 그 폴더 사진만 보인다. `전체`로 되돌릴 수 있다 | Must | F-5 |
| **여러 장 선택 → 폴더 이동** | 사진을 여러 장 골라 한 번에 폴더로 옮긴다 | Must | F-3, F-4 |
| 이미지 업로드 | 드래그 앤 드롭 + 파일 선택. 여러 장 동시 업로드(최대 10장/회 *(가정)*) | Must | C-2, C-4 |
| **업로드 대상 표시** | 드래그 중 "[셀프웨딩 · 야외]에 올라갑니다"로 목적지를 미리 보여준다 | Must | C-3 |
| 이미지 다운로드 | 사진에서 원본 화질 그대로 내려받기 | Must | E-4 |
| 링크 이미지 추출 | URL을 넣으면 대표 이미지·제목·출처를 자동으로 가져와 카드로 만든다 | Must | D-1, D-2 |
| 추출 실패 대비 | 이미지를 못 가져오면 기본 카드로 저장 + 직접 첨부 가능 | Must | D-3 |
| 댓글 | 사진마다 댓글을 달고, 작성자·시간이 보인다 | Must | E-2, E-3 |
| **저스티파이드 그리드** | 행마다 높이를 맞추고 폭은 원본 비율대로. 사진을 자르지 않는다 | Must | C-4, E-1 |
| 작성자·시간 표시 | 누가 언제 올렸는지 표시 | Must | C-5 |
| 내용 새로고침 | 화면을 새로고침하면 친구가 올린 최신 내용이 보인다 | Must | E-1 |
| 모바일 화면 대응 | 스마트폰 브라우저에서 보기·올리기·댓글이 모두 가능 | Must | 전체 |
| 삭제 | 자기가 올린 사진·댓글을 지울 수 있다 | Must | F-4 |

### 5-2. 있으면 좋음 (Nice to have)

| 기능명 | 설명 | 우선순위 | 관련 흐름 |
|---|---|---|---|
| 자동 실시간 갱신 | 새로고침 없이 친구가 올린 사진이 화면에 바로 나타남 | Nice | E-1 |
| **새 업로드 표시** | 새 사진에 파란 테두리 4초 + 카테고리 카드에 파란 점 | Nice | B-3, E-1 |
| **접속 중 표시** | 헤더에 지금 보드를 보고 있는 사람의 닉네임 + 점 | Nice | 전체 |
| **"새 사진 n장 ↑" 바** | 스크롤 중일 때 화면을 밀지 않고 상단에 알림만 표시 | Nice | E-1 |
| **카테고리 간 사진 이동** | 잘못 올린 사진을 다른 카테고리로 옮기기 | Nice | F-4 |
| 검색 | 폴더명·댓글·메모 안에서 찾기 | Nice | F-5 |
| 좋아요/이모지 반응 | 댓글 없이 가볍게 반응 | Nice | E-2 |
| 일괄 다운로드 | 폴더 전체를 ZIP으로 내려받기 | Nice | E-4 |
| 클립보드 이미지 붙여넣기 | Ctrl+V로 바로 업로드 | Nice | C-2 |
| 메모 | 사진에 짧은 설명 추가 | Nice | E-2 |
| **카테고리 대표 이미지 직접 지정** | 자동(최신 1장) 대신 원하는 사진을 대표로 고정 | Nice | B-2 |
| 사진 순서 바꾸기 | 드래그로 그리드 순서 조정 | Nice | C-4 |

---

## 6. 완료 기준

### 6-1. 기능 완료 기준 (기능이 "됐다"의 기준)

- [ ] 보드를 만들면 **10초 안에** 초대 링크가 생성되고, 복사 버튼 클릭 시 클립보드에 복사된다
- [ ] 초대 링크를 받은 사람이 **회원가입 없이 3번의 화면 터치 이내**로 보드에 들어온다
- [ ] 보드 홈 첫 화면에 **6개 카테고리가 한 줄로 전부** 보인다(1280px 기준, 가로 스크롤 없이)
- [ ] 각 카테고리 카드에 **최신 업로드 1장이 대표 이미지로** 표시되고, `[장수]`가 실제 장수와 일치한다
- [ ] 카테고리 카드를 클릭하면 **1초 이내**에 해당 카테고리 사진 그리드가 뜬다
- [ ] 이미지 **5장을 한 번에** 드래그해서 업로드하면, **10초 안에** 5장 모두 그리드에 나타난다
- [ ] 드래그하는 동안 **"[카테고리 · 폴더]에 올라갑니다"** 문구에 실제 목적지 이름이 표시된다
- [ ] 세로 사진과 가로 사진을 섞어 올려도 **잘리지 않고 원본 비율 그대로** 보인다
- [ ] 업로드한 이미지를 다운로드하면 **원본과 동일한 파일**(용량·화질 동일)이 저장된다
- [ ] 이미지 1장 업로드 후 **3초 이내**에 썸네일이 화면에 표시된다
- [ ] URL을 붙여넣으면 **5초 안에** 대표 이미지·제목·출처가 포함된 카드가 만들어진다
- [ ] 대표 이미지가 없는 URL을 넣어도 **에러 없이** 기본 카드로 저장된다
- [ ] 자주 쓰는 사이트 **5곳(인스타그램, 핀터레스트, 네이버 블로그, 유튜브, 일반 쇼핑몰) 중 4곳 이상**에서 이미지 추출에 성공한다 *(가정 — 대상 사이트는 확정 필요)*
- [ ] 한 카테고리 안에서 **폴더를 3개** 만들고, 폴더 칩 클릭 시 **1초 이내**에 해당 폴더 사진만 걸러진다
- [ ] 사진 **10장을 선택해 한 번에** 폴더로 옮길 수 있다
- [ ] 폴더를 삭제해도 **안의 사진은 지워지지 않고** `전체`에 그대로 남는다
- [ ] 사진에 댓글을 달면 **새로고침 없이** 즉시 화면에 나타난다
- [ ] A가 올린 사진을 B가 **새로고침 후 100% 확인**할 수 있다
- [ ] 아이폰·안드로이드 기본 브라우저에서 위 항목 전부가 동일하게 동작한다(모바일은 2열 그리드)

### 6-2. 사용성 완료 기준 (사용자 검증)

- [ ] 실제 친구 **2명**이 **7일간** 사용하고, 그 기간 동안 **사진 30장 이상 / 댓글 10개 이상**이 쌓인다
- [ ] **6개 카테고리 중 4개 이상**에 사진이 1장 이상 들어간다 *(카테고리 구분이 실제로 쓰이는지 확인)*
- [ ] 7일 후 두 사람 모두 "카톡으로 공유하던 때보다 편하다"에 **예**라고 답한다
- [ ] 사용 중 **데이터 유실(올린 사진이 사라짐) 0건**
- [ ] 아무 설명 없이 링크만 받은 사람이 **첫 사진 업로드까지 2분 이내** 도달한다
- [ ] 7일간 만들어진 **폴더가 1개 이상** 생긴다 *(0개면 폴더 기능은 과잉이었다는 뜻)*

---

## 7. 범위 제외

사용자가 "잘 모르겠다"고 답한 부분이라, 아래는 **PM 판단으로 제외** *(가정)* 했습니다. 기준은 **"2명이 쓰는 도구에 지금 당장 필요한가"** 입니다.

| 제외 항목 | 제외 이유 |
|---|---|
| **카테고리 추가·삭제·이름 변경** | 6개는 촬영 종류에 맞춰 이미 정해진 값이다. 고를 필요가 없어야 저장이 빨라진다. 필요해지면 v0.3에서 |
| **폴더 안의 폴더 (2단계 이상)** | 2명이 쓰는 보드에서 중첩 폴더는 "어디에 넣었더라"를 만든다 |
| **사진 1장에 여러 카테고리·폴더 지정** | 한 장 = 한 자리. 중복 지정은 "이건 어디 있지"를 만든다 |
| **다크 모드** | 배경 밝기가 바뀌면 같은 사진의 톤이 다르게 보인다. 레퍼런스 비교가 목적이므로 배경을 오프화이트로 고정한다 (`Reference-DESIGN.md` 2-2 참고) |
| **사진 자르기·비율 통일** | 스냅 레퍼런스는 프레이밍 자체가 참고 대상이라 원본 비율을 유지해야 한다 |
| 모바일 전용 앱 (iOS/Android) | 앱스토어 심사·배포 비용이 크다. 모바일 브라우저로 충분히 같은 경험을 줄 수 있다 |
| 공개 보드 / 탐색 기능 | 친구끼리 쓰는 비공개 도구다. 공개는 신고·차단·저작권 대응이 따라와 규모가 커진다 |
| 정식 회원가입·로그인 (이메일/소셜) | 비밀번호 찾기, 개인정보 보관 등 부담이 생긴다. MVP는 초대 링크 + 닉네임으로 충분 |
| 세밀한 권한 관리 (읽기 전용, 관리자 등) | 2명은 서로 다 믿는 사이다. 인원이 늘면 그때 필요 |
| AI 자동 태그·유사 이미지 추천 | 카드가 수백 장 쌓인 뒤에야 가치가 생긴다 |
| 이미지 편집 (자르기, 보정, 콜라주) | 별개의 제품 영역. 기존 도구로 편집 후 올리면 된다 |
| 동영상·PDF 등 이미지 외 파일 | 저장 용량과 재생 처리가 별도로 필요하다 |
| 실시간 커서·동시 편집 표시 | 구현 난도가 높고, 초대 인원이 적어 충돌할 일이 거의 없다 |
| 결제·유료 플랜 | 무료 도구로 검증이 먼저다 |
| 카드 버전 기록 / 휴지통 복구 | 삭제 시 확인창으로 대체 |
| 외부 서비스 연동 (슬랙, 노션 등) | 사용자 2명 시나리오에 해당 없음 |

---

## 8. 가정과 확인할 질문

### 8-1. 제가 추정해서 채운 내용 (가정)

| # | 가정한 내용 | 왜 이렇게 추정했나 |
|---|---|---|
| 1 | 사용 인원은 2~5명, 보드는 1개 *(가정)* | "친구, 나"라는 답변 기준 |
| 2 | 로그인은 초대 링크 + 닉네임만 *(가정)* | 진입 장벽을 최대한 낮춰야 "바로 올린다"는 목표가 산다 |
| 3 | "실시간"의 1차 기준은 **새로고침하면 보인다** *(가정)* | 자동 갱신은 구현 부담이 커서 Nice로 분리 |
| 4 | ~~폴더 = 서랍, 카테고리 = 딱지~~ → **카테고리 6개 고정 > 폴더(사용자 생성)** | **v0.2에서 확정됨.** 카테고리가 상위, 폴더가 하위 |
| 5 | 이미지 1장 최대 10MB, 보드 전체 2GB *(가정)* | 일반 웹 이미지·스크린샷 기준 |
| 6 | 링크 이미지 추출은 OG 태그 방식 *(가정)* | 가장 표준적이고 구현이 빠름 |
| 7 | 웹(모바일 브라우저 포함) 단일 플랫폼 *(가정)* | 앱 개발 비용 회피 |
| 8 | 카드는 최신순 정렬이 기본 *(가정)* | "방금 올린 것"을 보는 게 핵심 목적 |
| 9 | 댓글은 1단계(대댓글 없음) *(가정)* | 2명 대화에 대댓글은 과함 |
| 10 | 삭제는 본인이 올린 것만 가능 *(가정)* | 실수 방지 |
| 11 | 카테고리 대표 이미지는 최신 1장 자동 *(가정)* | 고르는 수고 없이 "최근에 뭐가 들어왔나"가 보임 |
| 12 | 사진 그리드는 저스티파이드(행 높이 통일) *(가정)* | 레퍼런스 2번 화면의 실제 구조. 행 단위로 끊겨 비교가 쉬움 |
| 13 | 히어로 문구는 `SNAP REFERENCE` *(가정)* | 한글은 얇은 웨이트에서 획이 뭉쳐 영문 대문자로 |
| 14 | 다크 모드 없음, 배경 오프화이트 고정 *(가정)* | 사진 톤 판단의 기준선을 하나로 고정 |

### 8-2. 결정해주셔야 할 질문

1. **"실시간"의 기준을 어디까지로 잡을까요?**
   ① 새로고침하면 보임(개발 빠름) / ② 자동으로 화면에 나타남(개발 기간 +약 3~5일 *(가정)*) — 원래 말씀하신 문제가 "실시간으로 확인 불가"였기 때문에, 이 답에 따라 제품의 체감이 크게 달라집니다.

2. **링크는 주로 어떤 사이트에서 가져오시나요?**
   인스타그램·핀터레스트는 이미지 추출이 자주 막힙니다. 실제로 쓰실 사이트 3~5곳을 알려주시면 그곳부터 확실히 되게 만들겠습니다.

3. **친구 외에 다른 사람이 링크를 알게 되면 어떤가요?**
   ① 링크만 알면 누구나 입장(간편, 노출 위험) / ② 4자리 입장 코드 추가(한 단계 더 번거로움) 중 선택이 필요합니다.

4. ~~**폴더와 카테고리, 둘 다 꼭 필요한가요?**~~ → **v0.2에서 해결됨.** 카테고리 6개 고정 + 그 안의 폴더로 확정.

5. **다운로드가 정말 자주 쓰이나요?**
   보기만 하면 되는지, 아니면 실제로 파일을 내려받아 다른 곳(PPT, 문서 등)에 쓰시는지에 따라 원본 보관 방식과 저장 용량 설계가 달라집니다.

6. **카테고리 대표 이미지를 직접 고르고 싶으신가요?**
   지금은 "가장 최근에 올린 1장"이 자동으로 올라옵니다. 보드 첫 화면의 인상이 매번 바뀌는 셈인데, 마음에 드는 한 장을 고정하는 방식이 더 나을 수도 있습니다.

7. **`친구·우정` 이라는 이름 그대로 갈까요?**
   카드 폭이 좁아 두 단어가 한 줄에 들어가려면 글자가 작아집니다. `우정` 한 단어로 줄이면 나머지 5개(개인/커플/일상스냅/셀프웨딩/웨딩스튜디오)와 글자 크기가 맞습니다.

---

## 【Review】

### ① 이 문서에서 논리적으로 가장 약한 부분

**"6개 카테고리가 실제 사진 분포와 맞는다는 근거가 없다"** 는 점입니다. 이 PRD와 디자인 전체가 "6개로 나뉜다"를 전제로 지어져 있습니다 — 보드 홈 한 줄 배치, 카드 폭, 고정 카테고리, 폴더의 위치까지 전부 여기에 매여 있습니다. 그런데 실제로는 **일상스냅 한 곳에 70%가 쏠리고 웨딩스튜디오는 3장뿐**일 수 있습니다. 그러면 홈 화면의 6칸 중 절반이 늘 비어 보이고, 정작 사진이 몰린 카테고리는 폴더 없이 감당이 안 됩니다.

이 가정은 **코드 없이 30분이면 검증됩니다**: 지금 각자 가지고 있는 레퍼런스 사진 30~50장을 6개 이름으로 직접 나눠보면 됩니다. v0.1에서 "Must가 많다"고 지적했던 문제는 계층이 확정되며 정리됐지만, 그 계층 자체의 근거는 아직 비어 있습니다.

부차적으로, Must 기능이 **20개로 늘었습니다**(v0.1은 14개). 카테고리·폴더·그리드 관련 항목이 추가된 결과이며, 1개월 일정과 맞는지는 개발자 산정이 필요합니다. `[확인 필요]`

### ② 개발자·디자이너가 반드시 물어볼 질문 2가지

**Q1. "실시간이라고 써 있는데, 서버를 계속 연결해두는 방식(웹소켓)인가요 아니면 주기적으로 물어보는 방식(폴링)인가요? 아니면 그냥 새로고침인가요?"**

> **A:** MVP는 **새로고침 기준**으로 만듭니다(Must). 자동 갱신은 Nice로 분리했고, 만든다면 웹소켓보다 **3~5초 주기 폴링**을 권합니다 — 인원이 5명 이하라 서버 부담이 거의 없고 구현이 훨씬 단순합니다. 단, 사용자가 처음 호소한 문제가 "실시간 확인 불가"였으므로, 질문 1번의 답에 따라 이 기능을 Must로 올릴 수 있습니다.

**Q2. "닉네임만으로 들어오면 같은 사람인지 어떻게 구분하죠? 브라우저를 껐다 켜면 다시 입력해야 하나요?"**

> **A:** 브라우저 저장소(localStorage)에 닉네임과 임시 식별자를 저장해, 같은 기기·같은 브라우저라면 다시 묻지 않습니다. 다만 **다른 기기에서 접속하면 다른 사람으로 인식**되는 한계가 있습니다(본인 카드 삭제 권한에 영향). 2명 시나리오에서는 감수 가능한 수준으로 판단했으나, 인원이 늘면 정식 로그인 도입이 필요합니다. `[확인 필요]`

### ③ 수치·사실 확인이 필요한 항목

| 항목 | 현재 기재값 | 확인해야 할 것 |
|---|---|---|
| 이미지 용량 상한 | 10MB / 보드 2GB | 실제 저장소 비용 산정 후 확정 `[확인 필요]` |
| 링크 추출 성공률 | 5곳 중 4곳 이상 | 대상 사이트 미확정. 인스타그램·핀터레스트는 로그인 요구로 추출이 막힐 가능성 높음 `[확인 필요]` |
| 자동 갱신 개발 기간 | +3~5일 | 개발자 산정 필요 `[확인 필요]` |
| 업로드 응답 시간 | 3초 / 5장 10초 | 서버 사양과 네트워크 환경 미정 `[확인 필요]` |
| MVP 출시 목표 | 1개월 | 투입 인원과 개발 가능 시간 미확인 `[확인 필요]` |

---

## 이 PRD에서 가장 먼저 검증해야 할 것 1가지

> **"지금 가지고 있는 레퍼런스 사진 30장을 6개 카테고리로 나눴을 때, 한 곳에 몰리지 않고 실제로 갈라지는가"** — 이 6개 구분 위에 홈 화면·업로드 흐름·폴더 위치까지 전부 올라가 있는데, 아직 사진 한 장으로도 확인된 적이 없습니다. 코드 없이 30분이면 끝납니다.

**함께 진행할 1일차 기술 확인**: 인스타그램·핀터레스트 URL 10개로 OG 태그 추출을 돌려봅니다. 이것이 안 되면 링크 카드가 전부 회색 빈 박스가 되어, 이 제품은 카톡보다 나을 게 없어집니다.

---

*Reference PRD v0.2 · 2026-09-17 · MAKER 공식 적용 · 디자인 명세: `Reference-DESIGN.md`*
