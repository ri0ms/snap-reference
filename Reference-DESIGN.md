# Reference — 디자인 명세서 (Design Spec)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 |
| 작성일 | 2026-09-17 |
| 대상 독자 | 디자이너 · 프론트엔드 개발자 |
| 함께 보는 문서 | `Reference-PRD.md` |
| 디자인 레퍼런스 | 사진작가 포트폴리오형 미니멀 에디토리얼 (첨부 이미지 2장) |

---

## 0. 디자인 원칙 5가지

이 사이트는 **스냅사진 레퍼런스를 모으는 곳**입니다. 화면이 예뻐 보이는 것보다, **올린 사진의 색·톤·분위기를 정확하게 판단할 수 있는 것**이 먼저입니다. 아래 5가지가 모든 결정의 기준입니다.

1. **색은 사진이 쓴다.** 배경·카드·버튼 같은 넓은 면은 무채색으로 고정합니다 — 여기에 색이 들어가면 사진의 색 판단이 틀어집니다. 색은 두 가지만 씁니다: **강조색(틸 그린)**은 제목과 누를 수 있는 것에, **실시간 신호(블루)**는 새 업로드 표시에. 둘 다 글자와 1~2px 선에만 얹고 면을 칠하지 않습니다.
2. **테두리보다 여백.** 사진과 카드는 직각 그대로 두고, 구분은 여백과 1px 실선으로 합니다. 둥근 모서리와 그림자는 **버튼에만** 허용합니다 — 누를 수 있는 것과 볼 것을 구분하는 유일한 장치이기 때문입니다.
3. **글자는 작고 얇게, 제목만 크게.** 본문·라벨은 11~14px로 조용히, 페이지 제목만 과감하게 큽니다. 레퍼런스의 대비 구조입니다.
4. **사진 비율을 자르지 않는다.** 세로 사진은 세로로, 가로 사진은 가로로 보여줍니다. 스냅 레퍼런스는 프레이밍 자체가 참고 대상이기 때문입니다.
5. **한 화면에 정보를 겹쳐 얹지 않는다.** 사진 위에 텍스트를 올리지 않고, 항상 사진 **바깥 아래**에 둡니다.

---

## 0-1. 보기 전용 모드 (`?view`)

제3자에게 보여줄 때 쓰는 링크입니다. 주소 뒤에 `?view` 를 붙입니다.

```
https://.../            ← 편집용 (우리)
https://.../?view       ← 보기 전용 (제3자)
```

| | 편집용 | 보기 전용 |
|---|---|---|
| 닉네임 입력 | 처음 1회 | **묻지 않음** — 링크 열면 바로 사진 |
| 사진 올리기 · 삭제 | O | 숨김 |
| `전체` · `+ 폴더` · 선택 체크박스 | O | 숨김 |
| 댓글 | O | **숨김** (내부 대화가 외부에 보이지 않도록) |
| `대표로 지정` | O | 숨김 |
| 접속자 이름 | O | 숨김 |
| 카테고리 이동 · 폴더 필터 · 크게 보기 · 다운로드 | O | **O** |
| 실시간 갱신 | O | O (참여자로는 잡히지 않음) |

> **화면에서만 감추는 방식입니다.** 서버 규칙(RLS)은 여전히 누구에게나 쓰기를 허용하므로, 개발자 도구를 다루는 사람은 우회할 수 있습니다. 막고자 하는 것이 **"실수로 지우는 것"** 이라면 충분하고, **"작정한 사람"** 이라면 Supabase Auth로 역할을 나눠야 합니다.

### 테스트 모드 (`?test`)

기능을 확인할 때 쓰는 링크입니다. **업로더 이름이 `__test__` 로 고정**되고, 닉네임을 묻지 않으며, 브라우저에 저장된 닉네임도 건드리지 않습니다.

```
https://.../?test
```

정리할 때는 **`__test__` 로 표시된 것만** 골라 지웁니다. 실제 사용자가 올린 사진·댓글과 절대 섞이지 않습니다.

```js
sb.from('photos').delete().eq('uploader','__test__')
sb.from('comments').delete().eq('who','__test__')
```

> 테스트 중에는 헤더 접속자 목록에 `__test__` 가 뜹니다. 누군가 보드를 손대고 있다는 뜻이니, 그대로 두시면 곧 정리됩니다.
>
> 사진 위에 뜨는 업로더 이름(`[지훈 · 방금]`)은 보기 전용에서도 보입니다. 가리려면 `#lb-meta`, `.tile .cap` 도 `body.ro` 규칙에 추가하세요.

---

## 1. 화면 구조 (Information Architecture)

```
홈 / 보드 (Board Home)          ← 레퍼런스 1번
│   대형 타이틀 + 6개 카테고리 카드 1행
│
└─ 카테고리 상세 (Category)     ← 레퍼런스 2번
   │   저스티파이드 그리드 + 폴더 칩 + 업로드
   │
   ├─ 폴더 필터 (같은 화면에서 전환, 페이지 이동 없음)
   │
   └─ 이미지 상세 (Lightbox, 오버레이)
          원본 크게 보기 + 댓글 + 다운로드
```

**깊이는 3단계까지만.** 보드 → 카테고리 → 이미지. 폴더는 별도 페이지가 아니라 **카테고리 안의 필터**입니다. 페이지를 더 파고들게 만들면 "3초 안에 저장"이라는 목표가 깨집니다.

### 분류 체계

| 계층 | 이름 | 개수 | 만들 수 있나 | 관계 |
|---|---|---|---|---|
| 1단계 | **카테고리** | 6개 고정 | 아니오 (초기 고정) | 이미지 1장 = 카테고리 1개 |
| 2단계 | **폴더** | 제한 없음 | 예 (사용자가 생성) | 이미지 1장 = 폴더 0~1개 |

> 폴더 안에 폴더는 만들 수 없습니다(1단계만). 2명이 쓰는 보드에서 폴더가 중첩되면 "어디에 넣었더라"가 시작됩니다.

### 6개 카테고리 (고정)

이름은 **두 가지 표기**를 함께 둡니다. 서체가 다르기 때문입니다 — `name`은 대문자(display 서체: 헤더 내비·상세 제목·안내 문구), `label`은 대소문자 혼용(script 서체: 카드 라벨).

| # | `name` (대문자) | `label` (카드) | 슬러그(URL) | 성격 |
|---|---|---|---|---|
| 1 | PERSONAL | *Personal* | `#/personal` | 개인 — 1인 인물 스냅 |
| 2 | COUPLE | *Couple* | `#/couple` | 커플 — 2인 커플 스냅 |
| 3 | FRIENDS | *Friends* | `#/friends` | 친구·우정 — 2인 이상 친구 |
| 4 | DAILY | *Daily* | `#/daily` | 일상스냅 — 야외·실내 데일리 |
| 5 | SELF WEDDING | *Self Wedding* | `#/self-wedding` | 셀프웨딩 — 야외 셀프 웨딩 |
| 6 | WEDDING STUDIO | *Wedding Studio* | `#/wedding-studio` | 웨딩스튜디오 — 실내 스튜디오 웨딩 |

---

## 2. 디자인 토큰

### 2-1. 색

```css
:root{
  /* 바탕 — 순백이 아닌, 아주 옅은 중성 오프화이트.
     순백은 사진 흰 부분을 삼키고, 크림색은 사진 화이트밸런스를 왜곡합니다. */
  --paper:      #F2F2F0;   /* 페이지 배경 */
  --surface:    #FFFFFF;   /* 입력창·모달 바탕 */
  --placeholder:#E6E6E3;   /* 사진 없는 칸 */

  /* 잉크 */
  --ink:        #111111;   /* 제목·본문 */
  --ink-60:     #6B6B68;   /* 보조 설명, 메타 */
  --ink-35:     #A5A5A0;   /* 비활성, 플레이스홀더 텍스트 */
  --line:       #DCDCD8;   /* 1px 구분선 */

  /* 강조색 — 제목과 "누를 수 있는 것"에만 */
  --accent:     #0D7370;   /* 틸 그린 */

  /* 실시간 신호 전용 */
  --live:       #2563EB;   /* 친구의 새 업로드, 진행 바, 접속 중 점 */
  --live-soft:  #DCE8FF;   /* 새 업로드 카드 잔광 */
}
```

### 강조색 `--accent` 를 쓰는 곳

| 위치 | 적용 |
|---|---|
| 히어로 `SNAP REFERENCE` | 글자색 |
| 닉네임 첫 화면 `REFERENCE` | 글자색 |
| 헤더 탭 — 호버 | 글자색 |
| 헤더 탭 — 선택됨 | 글자색 + **밑줄 2px** |
| 카드 라벨 — 호버 | 글자색 |

**쓰지 않는 곳** — 배경, 카드, 버튼 채움, 본문, 카테고리 상세 제목(`PERSONAL`은 검정 유지). 넓은 면에 칠하면 사진의 색 판단이 흐려집니다.

> **왜 틸 그린인가** — 인물 사진에는 청록이 거의 없습니다. 사진이 수백 장 쌓여도 UI 색과 섞이지 않는 유일한 색 계열입니다.
>
> **밝기가 핵심입니다.** 탭의 기본색은 `--ink-60`(#6B6B68)인데, 강조색이 이보다 훨씬 어두우면 눈은 "색이 생겼다"가 아니라 **"글자가 진해졌다"**로 읽습니다. `#0D7370`은 `--ink-60`과 밝기가 거의 같아(명도 대비 1.06) **바뀌는 것이 색조뿐**이라 색으로 분명히 읽힙니다. 강조색을 바꿀 때 이 조건을 깨지 마세요 — 짙은 남색·자주·올리브 계열은 전부 여기서 실패합니다.
>
> 탭 밑줄은 **모든 탭에 2px(투명)로 미리 깔아둡니다.** 선택될 때만 굵기를 키우면 글자가 1px 밀립니다.

**`--live` 사용 규칙 (이것만 지키면 됩니다)**

| 쓰는 곳 | 쓰지 않는 곳 |
|---|---|
| 새 업로드 카드 외곽선 (4초 후 사라짐) | 버튼 채움 |
| 업로드 진행 바 1px | 링크 색 |
| 친구 접속 중 점 4px | 아이콘 |
| 카테고리 카드의 "새 사진 있음" 점 | 강조 텍스트 |

### 2-2. 다크 모드

**만들지 않습니다.** 배경 밝기가 바뀌면 같은 사진이 다른 톤으로 보입니다. 레퍼런스를 비교하는 것이 목적이므로 배경은 `--paper` 하나로 고정합니다. (PRD 5-2의 "다크 모드 Nice" 항목은 이 판단에 따라 **제외로 이동**합니다.)

### 2-3. 타이포그래피

서체는 **두 가지 역할로 나눕니다.**

| 역할 | 서체 | 이유 |
|---|---|---|
| **디스플레이** (영문 제목) | **DM Serif Display** | 기본 웨이트가 이미 굵게 그려진 세리프. 곡선이 둥글고 세리프가 두툼해 크기를 줄여도 안정적으로 읽힙니다 |
| **스크립트** (카드 라벨) | **Parisienne** | 필기체. 카드 아래 라벨 한 곳에만 씁니다 — 사진 격자 위에 손글씨 한 줄이 얹히는 대비를 만듭니다 |
| **본문** (한글·설명·숫자) | **Pretendard** | 한글·영문 자소 높이가 맞고, 작은 크기에서 잘 버팁니다 |

```css
@import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css");
@import url("https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Parisienne&display=swap");

--font:    "Pretendard", "Noto Sans KR", -apple-system, "Malgun Gothic", sans-serif;
--display: "DM Serif Display", "Playfair Display", Georgia, serif;
--script:  "Parisienne", "Snell Roundhand", "Apple Chancery", cursive;
```

**어디에 무엇을 쓰는가**

| 위치 | 서체 | 크기 | 표기 |
|---|---|---|---|
| 히어로 `SNAP REFERENCE` | display | `clamp(34px, 9vw, 122px)` | 대문자 |
| 닉네임 첫 화면 `REFERENCE` | display | `clamp(32px, 7.4vw, 60px)` | 대문자 |
| 카테고리 상세 제목 `SELF WEDDING` | display | 34px | 대문자 |
| 헤더 브랜드 `Reference` | display | 16px | 대소문자 |
| **헤더 카테고리 탭 `Self Wedding`** | **script** | **20px** | **대소문자 혼용** |
| **카드 라벨 `Self Wedding`** | **script** | **22px** | **대소문자 혼용** |

> **필기체는 대문자만으로 쓰지 마세요.** 스크립트 서체는 글자끼리 이어지도록 설계돼 있어서, 대문자만 늘어놓으면 획이 연결되지 않고 읽히지 않습니다. 그래서 필기체가 들어가는 두 곳(헤더 탭, 카드 라벨)은 `Self Wedding` 처럼 대소문자를 섞고, 대문자 자리는 전부 display 서체가 맡습니다.
>
> **필기체를 18px 아래로 내리지 마세요.** 획이 뭉개져 읽히지 않습니다. 헤더 탭을 13.5px에서 20px로 올린 이유입니다.
>
> **필기체에는 밑줄 여백이 더 필요합니다.** 활성 탭의 1px 밑줄은 `padding-bottom: 6px`로 띄웁니다. 스크립트는 아래로 뻗는 획(`p`, `g`, `y`)이 길어서, 일반 서체의 2px로는 글자와 밑줄이 겹칩니다.
>
> DM Serif Display와 Parisienne은 **둘 다 웨이트가 400 하나뿐**입니다. `font-weight: 700` 같은 값을 주면 브라우저가 가짜 굵기를 만들어 획이 뭉개지니, 반드시 400으로 두세요.

| 역할 | 크기 | 웨이트 | 자간 | 비고 |
|---|---|---|---|---|
| **디스플레이** (SNAP REFERENCE) | `clamp(34px, 9vw, 122px)` | 400 DM Serif | `.006em` | 영문 대문자, 중앙. `line-height:.98` |
| **페이지 제목** (카테고리명) | 34px | 400 DM Serif | `.012em` | 영문 대문자 |
| **카드 라벨** (Self Wedding) | 22px | 400 Parisienne | `.01em` | 대소문자 혼용, `line-height:1.25` |
| **섹션 제목** | 18px | 400 | `.01em` | |
| **본문** | 14px / 1.7 | 400 | 0 | |
| **라벨·메타** | 11.5px | 500 | `.10em` | 영문은 대문자 |
| **숫자** | 11.5px | 500 | `.06em` | `font-variant-numeric: tabular-nums` |

**대괄호 표기법 — 이 사이트의 고유 규칙**
레퍼런스가 `[2019-2023]`으로 메타를 감싸듯, 이 사이트도 **숫자 메타는 항상 대괄호 안에** 넣습니다. 숫자만 덩그러니 놓는 것보다 "이건 사진이 아니라 설명"이라는 신호가 됩니다.

```
[ ALL · 5 PHOTOS ]   ← 보드 홈 전체 장수 (연도는 넣지 않음)
[128]                ← 카테고리 사진 수
[2026.09.17]         ← 업로드 날짜
[수아 · 방금]          ← 업로더
```

### 2-4. 간격 · 그리드

```css
--gutter-x: 26px;   /* 화면 좌우 여백 — 레퍼런스처럼 바짝 붙입니다 */
--gap:      16px;   /* 카테고리 카드 사이 */
--gap-grid: 18px;   /* 사진 그리드 사이 */
--max:      1600px; /* 콘텐츠 최대 폭 */
```

간격 단위는 **4의 배수**만 씁니다: `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96`.

### 2-5. 모션

| 대상 | 값 |
|---|---|
| 기본 전환 | `160ms cubic-bezier(.2,.6,.2,1)` |
| 이미지 페이드인 | `280ms ease-out`, opacity만 (위치 이동 없음) |
| 새 업로드 잔광 | `--live` 외곽선 1px → 4초 유지 → `600ms` 페이드아웃 |
| 호버 | 이미지 밝기 `-4%`만. 확대·기울임·그림자 **금지** |

`prefers-reduced-motion: reduce` 일 때 잔광은 색만 남기고 애니메이션은 모두 끕니다.

---

## 3. 공통 헤더

```
┌──────────────────────────────────────────────────────────────────────┐
│  Reference          개인 커플 친구 일상 셀프웨딩 스튜디오      지훈 ● 수아 ● │
│  ↑ 보드로            ↑ 카테고리 바로가기 (상세 페이지에서만 노출)   ↑ 접속중  │
└──────────────────────────────────────────────────────────────────────┘
  높이 56px · 배경 --paper · 하단 경계선 없음 · position: sticky
```

- **좌측** 보드 이름 `Reference` — 디스플레이 서체 15px / 800. 클릭 시 보드 홈.
- **중앙** 6개 카테고리 탭 — 필기체(script) 20px, 대소문자 혼용, **탭 사이 간격 30px**. 홈에서는 **숨김**(홈 자체가 카테고리 목록이므로 중복). 상세 페이지에서만 노출, 현재 카테고리는 밑줄 1px(`padding-bottom: 6px`로 띄움). 폭이 좁아지면 가로 스크롤.
- **우측 비움.** 레퍼런스의 `FB IN YT` 자리에 닉네임을 넣었다가 뺐습니다 — 내 이름은 내가 이미 아는 정보라 자리값을 못 합니다. 나중에 **친구가 지금 보고 있는지**를 표시하게 되면 그때 이 자리를 씁니다.
- 스크롤해도 고정. 배경은 불투명 `--paper` (블러 없음).

---

## 4. 화면 A — 보드 홈

레퍼런스 1번을 6칸으로 바꾼 화면입니다.

```
┌───────────────────────────────────────────────────────────────────────┐
│  Reference                                              지훈 ● 수아 ●  │
│                                                                       │
│                                                                       │
│                    S N A P   R E F E R E N C E                        │
│                          [ 총 342장 · 2026 ]                           │
│                                                                       │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                      │
│  │     │ │     │ │     │ │     │ │     │ │     │                      │
│  │ 사진 │ │ 사진 │ │ 사진 │ │ 사진 │ │ 사진 │ │ 사진 │                      │
│  │     │ │     │ │     │ │     │ │     │ │     │                      │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                      │
│  개인 [64] 커플 [58] 친구·우정 일상스냅 셀프웨딩 웨딩스튜디오                  │
│          ●새          [41]    [77]    [52]    [50]                    │
└───────────────────────────────────────────────────────────────────────┘
```

### 4-1. 히어로

- 문구: **`SNAP REFERENCE`** (영문 대문자, 자간 `.085em`)
  레퍼런스의 `SELECTED WORK` 자리입니다. 한글 "스냅 레퍼런스"는 Light 웨이트에서 획이 뭉쳐 보여 영문으로 갑니다.
- 바로 아래 `[ 총 342장 · 2026 ]` — 11.5px, `--ink-60`, 중앙.
  레퍼런스의 `[2019-2023]`과 같은 자리·같은 역할입니다. **총 장수를 여기에 두는 이유**: 보드에 들어온 사람이 "지난번보다 늘었나"를 첫 화면에서 즉시 알 수 있기 때문입니다.
- 상하 여백: 위 96px / 아래 64px.

### 4-2. 카테고리 카드 6개

| 속성 | 값 |
|---|---|
| 배치 | 1행 6열, `gap 16px` |
| 카드 폭 | `(100% - 5×16px) / 6` — 1600px 기준 약 245px |
| 사진 비율 | **3:4** (약 245 × 327px) |
| 모서리 | 직각 (radius 0) |
| 대표 이미지 | 기본은 **가장 최근 업로드 1장**(자동). 사진 상세에서 `대표로 지정`을 누르면 그 사진으로 고정 |
| 비어 있을 때 | `--placeholder` 채움 + 중앙에 `[비어 있음]` 11.5px `--ink-35` |

> **비율을 3:4로 정한 이유**: 레퍼런스 카드는 약 4:7의 긴 세로지만, 그 비율로 6칸을 깔면 카드 높이가 430px를 넘어 히어로와 카드가 한 화면에 함께 들어오지 못합니다. 첫 화면에서 **제목과 6개 카테고리가 모두 보이는 것**이 더 중요해 3:4로 줄였습니다.

**카드 아래 라벨** (사진 위에 얹지 않음, 10px 아래)

```
Self Wedding   [52]   ●
↑              ↑      ↑
Parisienne     대괄호   새 업로드 있으면 --live 점 4px, 내가 본 뒤엔 사라짐
22px / 400     11.5px
대소문자 혼용     500 / --ink-60 / Pretendard
```

> 필기체는 **헤더 탭과 이 카드 라벨** 두 곳에만 씁니다. 둘 다 카테고리 이름이라 같은 옷을 입는 것이 맞습니다. 사진 격자가 전부 직각·무채색이라 손글씨가 들어갈 때 대비가 생기는데, **제목·본문·숫자까지 필기체로 늘리면 이 효과가 사라집니다.**

**호버**: 사진 밝기 `-4%`, 카테고리명 `--ink` 유지(굵어지지 않음). 커서 `pointer`.
**포커스**: 카드 전체에 `outline: 1px solid var(--ink); outline-offset: 3px`.

### 4-3. 하단 슬라이더

레퍼런스에 있는 가로 슬라이더는 **카드가 화면에 다 들어오지 않을 때만** 보여줍니다.

- 6칸이 모두 보이는 **1280px 이상**: 슬라이더 **숨김** (움직일 게 없는 컨트롤은 장식입니다)
- 1024~1279px: 가로 스크롤 + 슬라이더 노출
- 트랙 1px `--line`, 폭 200px, 핸들 지름 11px `--surface` + 1px `--ink` 테두리, 중앙 정렬

---

## 5. 화면 B — 카테고리 상세

레퍼런스 2번 화면입니다. **업로드와 보기가 같은 화면에서 일어납니다.**

```
┌───────────────────────────────────────────────────────────────────────┐
│  Reference    개인 커플 친구·우정 일상스냅 셀프웨딩 웨딩스튜디오   지훈 ● 수아 ● │
│               ────                                                    │
│                                                                       │
│  개인 [64]                                              ↑ 사진 올리기  │
│  ─────────────────────────────────────────────────────────────────    │
│  전체  야외  실내  필름톤  + 폴더                                        │
│  ────                                                                 │
│                                                                       │
│  ┌──────┐ ┌──────┐ ┌────────────────────┐                             │
│  │      │ │      │ │                    │   ← 1행: 높이 통일, 폭은 비율대로│
│  └──────┘ └──────┘ └────────────────────┘                             │
│  ┌────────────────┐ ┌──────┐ ┌──────────┐                             │
│  │                │ │      │ │          │                             │
│  └────────────────┘ └──────┘ └──────────┘                             │
└───────────────────────────────────────────────────────────────────────┘
```

### 5-1. 페이지 제목 줄

- 좌: `PERSONAL` 34px / display + `[64]` 11.5px `--ink-60`
- 우: `↑ 사진 올리기` (텍스트 버튼 13px, 호버 시 밑줄) + `전체` `삭제` (흰색 알약 버튼)
- 아래 1px `--line` 실선 (레퍼런스의 절제된 구분 방식)

**흰색 알약 버튼 (`.pill`)** — `+ 폴더` · `전체` · `삭제` 세 곳에 씁니다.

```css
.pill{
  background:#FFFFFF; color:#111; border-radius:999px;
  padding:6px 16px; font-size:13px;
  box-shadow:0 1px 2px rgba(17,17,17,.05), 0 2px 10px rgba(17,17,17,.05);
}
.pill.on   { background:#111; color:#F2F2F0 }  /* 켜진 상태 */
.pill.quiet{ color:#A5A5A0 }                   /* 지금 누를 수 없음 */
```

> `↑ 사진 올리기`만 알약이 아닌 텍스트입니다. **이 화면의 주된 행동**이라 나머지 보조 버튼과 모양을 달리해 위계를 만듭니다.

**알약과 텍스트 버튼을 한 줄에 놓을 때** — 상자 높이가 아니라 **글자 높이**를 맞춰야 합니다. 폴더 칩(`전체`·`야외`)은 활성 표시용 밑줄 때문에 아래쪽에 `2px 여백 + 1px 밑줄`이 붙습니다. 이대로 `align-items: center`를 걸면 상자는 가운데지만 글자는 위로 2~3px 치우쳐 보입니다.

```css
/* 위 3px = 아래 2px + 밑줄 1px — 대칭을 만들어 글자 높이를 알약과 맞춤 */
.chips button      { padding:3px 0 2px; border-bottom:1px solid transparent }
.chips button.pill { padding:6px 16px; border-bottom:0 }
```

> `.chips button` 이 `.pill` 보다 선택자 우선순위가 높아서, 두 번째 줄을 빼면 알약의 아래 여백이 `2px`로 덮어써집니다. **알약 안 글자가 위로 치우쳐 보이면 이 충돌을 먼저 확인하세요.**

**버튼 동작**

| 버튼 | 아무것도 선택 안 했을 때 | 선택된 사진이 있을 때 |
|---|---|---|
| `전체` | 지금 보이는 사진을 **전부 선택** | 전부 선택된 상태면 **해제**. 버튼은 `.on`(검정)으로 |
| `삭제` | `.quiet`(회색). 누르면 체크박스를 드러내고 **"지울 사진 선택"** 을 1.8초 표시 | `삭제 (12)` 처럼 장수 표시. 누르면 확인창 |

> `전체` 버튼의 글자는 **상태와 무관하게 항상 "전체"** 입니다. 켜짐/꺼짐은 글자가 아니라 배경색으로 알립니다 — 글자가 바뀌면 누르기 전에 무슨 일이 일어날지 예측하기 어려워집니다.

### 5-2. 폴더 칩 행

```
전체   야외   실내   필름톤   + 폴더
────
```

| 속성 | 값 |
|---|---|
| 형태 | 알약형 버튼 아님. **텍스트 + 활성 시 아래 1px 실선**. 칩 박스는 UI 색을 늘립니다 |
| 크기 | 13px / 400 / 간격 20px |
| 기본값 | `전체` 활성 |
| 개수 | `전체`를 제외한 폴더가 8개를 넘으면 가로 스크롤 |
| `+ 폴더` | 클릭 → 그 자리에 인라인 입력창(밑줄 1px). Enter 생성, Esc 취소 |
| 폴더 이름 | 최대 12자. 이미 있는 이름이면 입력창 아래 11.5px `--ink-60`로 "같은 이름의 폴더가 있습니다" |
| 폴더 수정·삭제 | 폴더명 우클릭 또는 길게 누르기 → `이름 바꾸기 / 삭제`. 삭제 시 안의 사진은 지워지지 않고 `전체`로 빠짐 (문구로 명시) |

### 5-3. 사진 그리드 — 저스티파이드 그리드

레퍼런스 2번은 메이슨리(세로 벽돌쌓기)가 **아닙니다**. 한 행 안에서 사진들의 **높이가 같고 폭이 제각각**입니다. 같은 방식으로 만듭니다.

| 속성 | 값 |
|---|---|
| 방식 | 행 단위 저스티파이드 — 행의 목표 높이를 정하고, 각 사진 폭 = `높이 × 원본비율`, 행 합계가 컨테이너 폭이 되도록 높이를 미세 조정 |
| 목표 행 높이 | 데스크톱 **260px** (허용 범위 200~330px) |
| 사진 간격 | 18px (가로·세로 동일) |
| 마지막 행 | 남는 칸을 늘려 채우지 않고 **왼쪽 정렬로 그대로 둠**. 억지로 늘리면 마지막 사진만 비정상적으로 커집니다 |
| 자르기 | 없음. 원본 비율 유지 |
| 정렬 | 최신순 고정 (PRD 가정 8번) |

**왜 메이슨리가 아닌가**: 메이슨리는 세로로 읽히고, 스크롤할 때 사진들이 서로 다른 위치에서 튀어나옵니다. 저스티파이드 그리드는 **행 단위로 눈이 끊기기 때문에** 여러 장을 나란히 비교하기 쉽습니다. 레퍼런스 비교가 목적인 이 제품에 맞습니다.

**사진 카드**

- 기본: 사진만. 테두리·그림자·라운드 없음
- 호버: 밝기 `-4%` + **좌하단 바깥**이 아니라 사진 위 하단에 그라데이션 없이 `[수아 · 09.17]` 11.5px 흰색 — 단, **호버했을 때만** 보입니다 (원칙 5의 유일한 예외, 그 외 텍스트는 사진 밖)
- 선택 모드: 좌상단 체크 박스 14px 사각(라운드 0)

### 5-4. 업로드

**세 가지 경로 모두 지원합니다.**

1. **드래그 앤 드롭** — 화면 어디든. 파일이 들어오면 전체 화면 오버레이:
   ```
   ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
   │                                 │   1px 점선 --ink
   │        여기에 놓으면              │   바탕 --paper 92% 불투명
   │        [개인 · 야외]에 올라갑니다   │   18px / 300, 중앙
   │                                 │   * 현재 카테고리와 선택된 폴더를 문구에 박아
   └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘     "어디에 올라가는지"를 놓기 전에 알려줍니다
   ```
2. **`↑ 사진 올리기` 버튼** → 파일 선택창
3. **붙여넣기** `Ctrl/Cmd + V` — 캡처한 이미지를 바로 (PRD 5-2 Nice)

**업로드 중 표시**

- 그리드 **맨 앞에 자리를 먼저 만들고** `--placeholder`로 채웁니다 (사진 비율은 파일에서 즉시 읽어 반영 → 레이아웃이 나중에 튀지 않음)
- 자리 하단에 1px `--live` 진행 바
- 실패 시: 그 자리를 `--placeholder`로 두고 중앙에 `올리지 못했습니다 · 다시 시도` (텍스트 버튼). 사과 문구·느낌표·빨간색 없음

### 5-5. 실시간 표시

| 상황 | 표현 |
|---|---|
| 친구가 지금 보드를 보고 있음 | 헤더 우측 닉네임 옆 `--live` 점 4px |
| 친구가 사진을 올림 | 그리드 맨 앞에 삽입 + 1px `--live` 외곽선 **4초** 후 페이드아웃 |
| 내가 다른 카테고리에 있을 때 | 헤더의 해당 카테고리 이름 옆 `--live` 점 4px. 그 카테고리에 들어가면 사라짐 |
| 내가 스크롤을 내리고 있을 때 | 화면을 밀어내지 않음. 상단에 `새 사진 3장 ↑` 텍스트 바(높이 32px, 배경 `--live-soft`) → 클릭하면 맨 위로 |

> 마지막 항목이 중요합니다. 보고 있는 화면이 갑자기 밀리면 "실시간"은 편리함이 아니라 방해가 됩니다.

### 5-6. 선택 모드 (폴더 분류 · 카테고리 이동)

사진 위 체크박스를 눌러 여러 장 선택 → **하단 고정 액션 바**

```
┌─────────────────────────────────────────────────────────────────────────┐
│  12장 선택   폴더로 이동   카테고리 옮기기   다운로드   삭제      선택 해제 │
└─────────────────────────────────────────────────────────────────────────┘
   높이 52px · 배경 --surface · 상단 1px --line · 하단 고정
```

- `폴더로 이동` → 기존 폴더 목록 + `+ 새 폴더로 이동`
- `카테고리 옮기기` → **현재 카테고리를 뺀 5개**만 나옵니다. 옮기면 폴더 지정이 비워집니다 — 폴더는 카테고리에 속하므로 따라갈 수 없습니다. 다른 사람 화면에도 즉시 반영됩니다
- `삭제`는 **본인이 올린 사진만** 활성. 남의 사진이 섞여 있으면 "내가 올린 8장만 지울 수 있습니다"를 문구로 표시
- 삭제 확인창: 빨간 버튼 없음. `삭제` 텍스트 버튼 + `취소`

---

## 6. 화면 C — 이미지 상세 (라이트박스)

사진을 클릭하면 페이지 이동 없이 오버레이로 뜹니다.

```
┌─────────────────────────────────────────────────────────┐
│                                              ×          │
│   ┌───────────────────────┐   개인 · 야외                 │
│   │                       │   [수아 · 2026.09.17]         │
│   │       원본 사진         │   ───────────────────        │
│   │                       │   댓글                       │
│   │                       │   지훈  이 톤 좋다              │
│   └───────────────────────┘   수아  배경이 좀 복잡한 듯      │
│         ‹        ›             ┌─────────────────────┐    │
│                                │ 댓글 쓰기…           │    │
│                                └─────────────────────┘    │
│                                ↓ 원본 다운로드            │
└─────────────────────────────────────────────────────────┘
```

| 속성 | 값 |
|---|---|
| 배경 | `--paper` 96% 불투명 (검정 딤 아님 — 사진 톤 판단을 위해) |
| 사진 영역 | 좌측, 최대 `min(72vw, 높이 88vh)` |
| 정보 패널 | 우측 320px 고정. 1024px 미만에서는 사진 아래로 내려옴 |
| 이동 | `‹ ›` 또는 키보드 `←` `→` |
| 닫기 | `×` · `Esc` · 배경 클릭 · **휴대폰 뒤로 가기** |
| 카테고리 옮기기 | 이 사진을 다른 카테고리로. 메뉴에는 **현재 카테고리를 뺀 5개**만 나오고, 옮기면 폴더 지정은 비워집니다(폴더는 카테고리에 속하므로) |
| 댓글 | 1단계만(대댓글 없음). 입력 후 Enter 전송, Shift+Enter 줄바꿈 |
| 댓글 삭제 | **내가 쓴 댓글에만** 오른쪽 끝에 `×`. 평소엔 숨어 있다가 그 줄에 마우스를 올리면 나타남(터치 기기는 항상 표시). 누르면 확인창 |
| **대표로 지정** | 이 사진을 카테고리 대표(홈 카드 이미지)로 고정. 이미 대표면 `★ 대표 사진`으로 강조색 표시되고, 다시 누르면 해제되어 **가장 최근 사진으로 되돌아감**. 지정은 모두에게 공유되고, 대표 사진을 지우면 자동으로 최신 사진이 대표가 됨 |
| 다운로드 | 텍스트 버튼. 업로드 시 긴 변 2400px로 줄인 파일 |

---

## 7. 반응형

| 구간 | 보드 홈 카테고리 | 카테고리 그리드 | 헤더 |
|---|---|---|---|
| **≥1280px** | 6열 1행, 슬라이더 숨김 | 저스티파이드, 목표 행 높이 260px | 전체 노출 |
| **1024~1279** | 6열 가로 스크롤 + 슬라이더 | 목표 행 높이 220px | 전체 노출 |
| **768~1023** | 3열 2행 | 목표 행 높이 180px | 카테고리 가로 스크롤 |
| **<768 (모바일)** | **2열 3행** | **2열 고정 그리드** (저스티파이드 해제) | 로고 + 햄버거 |

**모바일에서 저스티파이드를 포기하는 이유**: 폭이 좁으면 한 행에 1~2장밖에 못 들어가 행 높이가 널뛰고, 세로 사진 한 장이 화면을 다 먹습니다. 2열 고정 그리드(비율 유지, 세로 길이는 제각각인 단순 메이슨리)가 훑어보기에 낫습니다.

**모바일 세부**
- 화면 좌우 여백 16px, 사진 간격 8px
- `↑ 사진 올리기` 버튼은 우하단 고정 원형 아님 — 제목 줄 우측 텍스트 그대로 유지 (원형 플로팅 버튼은 사진을 가립니다)
- 하단 안전영역 `env(safe-area-inset-bottom)` 만큼 액션 바에 패딩 추가

---

## 8. 상태별 화면 (빈 화면 · 오류)

| 상황 | 화면 | 문구 |
|---|---|---|
| 보드가 완전히 비어 있음 | 카드 6개 모두 `--placeholder` | 히어로 아래: `첫 사진을 올려보세요. 카테고리를 고르면 시작됩니다.` |
| 카테고리가 비어 있음 | 그리드 자리에 점선 1px 영역(높이 260px) | `아직 사진이 없습니다. 파일을 여기로 끌어다 놓으세요.` |
| 폴더가 비어 있음 | 동일 | `이 폴더는 비어 있습니다. 사진을 선택해 옮겨보세요.` |
| 업로드 실패 | 해당 자리만 플레이스홀더 유지 | `올리지 못했습니다 · 다시 시도` |
| 파일 형식 안 맞음 | 오버레이 문구 교체 | `이미지 파일만 올릴 수 있습니다 (JPG, PNG, WEBP, HEIC)` |
| 용량 초과 | 동일 | `한 장에 10MB까지 올릴 수 있습니다` |
| 링크 이미지 추출 실패 | 회색 카드 생성 | `이미지를 못 가져왔습니다 · 직접 넣기` |
| 연결 끊김 | 헤더 우측 점이 `--ink-35`로 | `연결 끊김 · 다시 연결 중` |

**문구 규칙**: 사과하지 않고, 무엇이 안 됐는지와 다음에 뭘 누르면 되는지만 씁니다. 느낌표·이모지 쓰지 않습니다.

---

## 9. 접근성

- 모든 이미지에 `alt`: 업로더가 메모를 적었으면 메모, 없으면 `개인 카테고리 사진, 수아 업로드, 2026년 9월 17일`
- 키보드만으로 전 기능 조작 가능. 포커스 링은 `1px solid var(--ink)` + `offset 3px` (그림자형 링 아님)
- 본문 대비비 `--ink`/`--paper` = 약 16:1, `--ink-60`/`--paper` = 약 5.2:1 — 둘 다 AA 통과. **`--ink-35`는 장식·비활성에만** 쓰고 읽어야 하는 글자에는 쓰지 않습니다
- `--live` 점은 색만으로 정보를 전달하므로, 항상 **위치(이름 옆)와 숫자(`새 사진 3장`)를 함께** 제공합니다
- 클릭 대상 최소 크기: 데스크톱 32×32px, 모바일 44×44px

---

## 10. 개발 참고

### 10-1. 컴포넌트 목록

| 컴포넌트 | 쓰이는 곳 | 비고 |
|---|---|---|
| `Header` | 전 화면 | 카테고리 링크 노출 여부 prop |
| `HeroTitle` | 보드 홈 | 대괄호 메타 슬롯 포함 |
| `CategoryCard` | 보드 홈 | 대표 이미지 + 이름 + `[n]` + live dot |
| `CategorySlider` | 보드 홈 | ≥1280에서 미렌더 |
| `FolderChips` | 카테고리 상세 | 인라인 생성 입력 포함 |
| `JustifiedGrid` | 카테고리 상세 | 비율 배열 → 행 분배 계산 |
| `PhotoTile` | 그리드 | 선택 체크 / 업로드 진행 / live 잔광 상태 |
| `DropOverlay` | 카테고리 상세 | 전역 드래그 이벤트 |
| `SelectionBar` | 카테고리 상세 | 하단 고정 |
| `Lightbox` | 이미지 상세 | 댓글 패널 포함 |
| `EmptyState` | 여러 곳 | 문구만 교체 |

### 10-2. 저스티파이드 그리드 계산 (의사코드)

```js
// photos: [{ id, ratio }]  ratio = 원본 width / height
function layoutRows(photos, containerW, targetH, gap) {
  const rows = []; let row = [], sumRatio = 0;
  for (const p of photos) {
    row.push(p); sumRatio += p.ratio;
    // 이 행을 컨테이너 폭에 맞췄을 때의 실제 높이
    const h = (containerW - gap * (row.length - 1)) / sumRatio;
    if (h <= targetH) { rows.push({ items: row, height: h }); row = []; sumRatio = 0; }
  }
  // 마지막 행은 늘리지 않고 targetH 그대로 왼쪽 정렬
  if (row.length) rows.push({ items: row, height: targetH, last: true });
  return rows;
}
```

- **원본 비율은 업로드 시점에 서버에 저장**해 둡니다. 이미지를 받아봐야 비율을 아는 구조면 첫 렌더에서 레이아웃이 튑니다.
- 썸네일은 목표 행 높이의 2배(520px 높이)로 생성해 고해상도 화면까지 커버합니다.

### 10-3. 이미지 처리

| 항목 | 값 |
|---|---|
| 허용 형식 | JPG, PNG, WEBP, HEIC (HEIC는 업로드 시 JPG 변환) |
| 원본 보관 | 그대로 보관 (다운로드가 Must이므로 재압축 금지) |
| 썸네일 | 폭 기준 400 / 800 / 1600px 3종, WEBP |
| EXIF | 회전 정보만 적용 후 위치정보는 **제거하고 저장** |
| 지연 로딩 | 화면 아래 사진은 `loading="lazy"`, 비율 박스를 미리 잡아 CLS 0 유지 |

---

## 11. 다음에 결정할 디자인 사항

1. **보드 홈 히어로 문구** — `SNAP REFERENCE`로 제안했습니다. 다른 문구를 원하시면 알려주세요 (영문 대문자 12~16자가 레퍼런스 비율에 맞습니다).
2. **카테고리 대표 이미지** — 자동(최신 1장)으로 잡았습니다. 직접 고르는 방식이 필요한지 확인이 필요합니다.
3. **6개 카테고리 순서** — 요청하신 순서(개인→커플→친구우정→일상스냅→셀프웨딩→웨딩스튜디오)를 고정으로 두었습니다. 사진이 많은 순으로 자동 정렬하는 방식도 가능합니다.
4. **`친구·우정` 표기** — 카드 폭에 두 단어가 들어가야 해 가운뎃점으로 묶었습니다. `우정`만 쓰는 편이 짧고 읽기 쉽습니다.

---

*Reference 디자인 명세서 v0.1 · 2026-09-17*
